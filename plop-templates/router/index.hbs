import Layout from '@/layout'
export default {
  path: '/{{ name }}',
  component: Layout,
  children: [
    {
      // 默认路由,访问父路径,默认访问为空的路径的子页面''
      path: '',
      name: '{{ name }}',
      component: () => import('@/views/{{ name }}/index.vue'),
      meta: { title: '{{ title }}' }
    }
  ]
}
